const status=document.querySelector('#estado'),login=document.querySelector('#login'),actions=document.querySelector('#acoes');
async function send(path,method='GET',body){const response=await fetch(path,{method,credentials:'same-origin',headers:body?{'content-type':'application/json'}:{},body:body?JSON.stringify(body):undefined}),result=await response.json();if(!response.ok)throw new Error(result.erro||result.error||'Operação não concluída');return result}
async function credential({credential}){try{await send('/api/auth/google','POST',{credential});location.reload()}catch(error){status.textContent=error.message==='conta_nao_autorizada'?'Esta conta Google não está autorizada a acessar o sistema.':error.message}}
async function start(){try{await send('/api/sessao');login.hidden=true;actions.hidden=false}catch{const config=await send('/api/config');const wait=setInterval(()=>{if(globalThis.google?.accounts?.id){clearInterval(wait);google.accounts.id.initialize({client_id:config.googleClientId,callback:credential});google.accounts.id.renderButton(document.querySelector('#google-signin'),{theme:'outline',size:'large',text:'signin_with'})}},50)}}
document.querySelector('#sair').onclick=async()=>{await send('/api/logout','POST',{});location.reload()};start();
