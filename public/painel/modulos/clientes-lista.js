import {api} from "./api.js";import {escape,empty,loading,toast,confirmAction} from "./componentes.js";
export async function clientes(root,query=new URLSearchParams()){root.innerHTML=loading();const data=await api(`/clientes?${query}`);root.innerHTML=`<div class="page-head"><div><p class="eyebrow">Cadastros</p><h1>Clientes</h1><p class="muted">${data.total} cliente(s) encontrado(s)</p></div><a href="#/clientes/novo"><button>Adicionar cliente</button></a></div><form id="filters" class="toolbar panel"><input name="q" type="search" placeholder="Nome, ID, CRECI, cidade…" value="${escape(query.get("q")||"")}"><select name="status"><option value="">Todos os status</option><option value="ativo">Ativos</option><option value="inativo">Inativos</option><option value="suspenso">Suspensos</option></select><select name="feed"><option value="">Todos os feeds</option><option value="com">Com feed</option><option value="sem">Sem feed</option></select><button>Filtrar</button></form><section class="panel table-wrap">${data.itens.length?`<table><thead><tr><th>Cliente</th><th>Tipo</th><th>Cidade</th><th>Status</th><th>Feed</th><th>Ações</th></tr></thead><tbody>${data.itens.map(x=>`<tr><td><strong>${escape(x.nome)}</strong><br><small>${escape(x.id)}</small></td><td>${escape(x.tipo)}</td><td>${escape(x.cidade||"—")}</td><td><span class="badge ${escape(x.status||"")}">${escape(x.status||(x.ativo===false?"inativo":"ativo"))}</span></td><td>${x.feed.configurado?escape(x.feed.status):"Não configurado"}</td><td><a href="#/clientes/${encodeURIComponent(x.id)}">Administrar</a></td></tr>`).join("")}</tbody></table>`:empty("Nenhum cliente","Cadastre o primeiro cliente ou altere os filtros.",'<a href="#/clientes/novo"><button>Cadastrar cliente</button></a>')}</section>`;const status=root.querySelector('[name="status"]');status.value=query.get("status")||"";const feed=root.querySelector('[name="feed"]');feed.value=query.get("feed")||"";root.querySelector("#filters").onsubmit=e=>{e.preventDefault();location.hash=`#/clientes?${new URLSearchParams(new FormData(e.currentTarget))}`}}
export async function changeStatus(id,status){if(!await confirmAction(`${status[0].toUpperCase()+status.slice(1)} cliente`,`Essa alteração será persistida no repositório privado e poderá ser revertida.`))return;const result=await api(`/clientes/${id}/${status}`,{method:"POST",body:{}});toast(`Cliente ${result.status}.`);location.reload()}
