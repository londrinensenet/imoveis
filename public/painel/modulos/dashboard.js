import {api} from "./api.js";import {escape,loading,toast,confirmAction} from "./componentes.js";
export async function dashboard(root){root.innerHTML=loading();const data=await api("/dashboard");const metrics=[["Clientes",data.clientes.total],["Clientes ativos",data.clientes.ativos],["Clientes inativos",data.clientes.inativos],["Suspensos",data.clientes.suspensos],["Feeds",data.feeds.total],["Feeds com erro",data.feeds.com_erro],["Imóveis publicados",data.imoveis.publicados],["Imóveis rejeitados",data.imoveis.rejeitados]];root.innerHTML=`<div class="page-head"><div><p class="eyebrow">Operação</p><h1>Visão geral</h1><p class="muted">Dados administrativos atuais do repositório privado.</p></div><div class="actions"><a href="#/clientes/novo"><button>Adicionar cliente</button></a><button id="sync">Sincronizar todos</button></div></div><section class="cards">${metrics.map(([x,n])=>`<article class="card metric"><span>${escape(x)}</span><strong>${Number(n)||0}</strong></article>`).join("")}</section><section class="panel"><h2>Sincronização</h2><p>Última execução: <strong>${data.sincronizacao.ultima?escape(data.sincronizacao.ultima):"Ainda não executado"}</strong></p><p>Próxima execução: <strong>${data.sincronizacao.proxima?escape(data.sincronizacao.proxima):"Ainda não informada"}</strong></p><a href="#/diagnostico">Abrir diagnóstico do sistema →</a></section>`;root.querySelector("#sync").onclick=async()=>{if(!await confirmAction("Sincronizar todos","O workflow processará todos os clientes ativos. Deseja enviar a solicitação?"))return;try{const r=await api("/sincronizar",{method:"POST",body:{}});toast(r.mensagem)}catch(e){toast(e.message,"erro")}}}
