import {api} from './api.js';import {toast,confirmAction,escape} from './componentes.js';
export async function sincronizacoes(root){const d=await api('/dashboard');root.innerHTML=`<div class="page-head"><div><p class="eyebrow">Operação</p><h1>Sincronizações</h1></div><button id="sync">Sincronizar todos agora</button></div><section class="cards"><article class="card metric"><span>Feeds ativos</span><strong>${d.feeds.ativos}</strong></article><article class="card metric"><span>Com erro</span><strong>${d.feeds.com_erro}</strong></article><article class="card metric"><span>Em execução</span><strong>${d.sincronizacao.em_execucao}</strong></article></section><section class="panel"><h2>Histórico e erros</h2>${d.sincronizacao.falhas_recentes.length?`<ul>${d.sincronizacao.falhas_recentes.map(escape).join('')}</ul>`:'<p class="muted">Nenhuma falha recente registrada.</p>'}</section>`;root.querySelector('#sync').onclick=async()=>{if(await confirmAction('Sincronizar todos','Disparar o workflow geral agora?')){const r=await api('/sincronizar',{method:'POST',body:{}});toast(r.mensagem)}}}
export async function conta(root){const me=await api('/me');root.innerHTML=`<div class="page-head"><h1>Minha conta</h1></div><form id="profile" class="panel form-grid"><label>ID<input value="${escape(me.id)}" disabled></label><label>Nome<input name="nome" value="${escape(me.nome)}" required></label><label>Perfil<input value="${escape(me.role)}" disabled></label><button>Salvar nome</button></form><form id="password" class="panel form-grid"><h2>Alterar senha</h2><label>Senha atual<input name="senhaAtual" type="password" required></label><label>Nova senha<input name="novaSenha" type="password" minlength="10" required></label><label>Confirmar<input name="confirmacao" type="password" minlength="10" required></label><button>Alterar senha</button></form><section class="panel"><button id="logout-account" class="secondary">Encerrar sessão</button></section>`;root.querySelector('#logout-account').onclick=()=>document.querySelector('#sair').click();root.querySelector('#profile').onsubmit=async e=>{e.preventDefault();await api('/me',{method:'PUT',body:Object.fromEntries(new FormData(e.currentTarget))});toast('Nome atualizado')};root.querySelector('#password').onsubmit=async e=>{e.preventDefault();await api('/me/password',{method:'PUT',body:Object.fromEntries(new FormData(e.currentTarget))});e.currentTarget.reset();toast('Senha alterada')}}
export function configuracoes(root){root.innerHTML=`<div class="page-head"><h1>Configurações</h1></div><section class="cards"><article class="card"><h2>GitHub</h2><p>Integração realizada exclusivamente pelo Worker. Tokens nunca são enviados ao navegador.</p></article><article class="card"><h2>Workflows</h2><p>Sincronizações geral e individual configuradas.</p></article><article class="card"><h2>Publicação</h2><p>Visitantes acessam somente os artefatos estáticos do Pages.</p></article></section>`}
