// finalidade tipo preco toLocaleLowerCase — integração integral com o motor existente.
import{loadCards,fetchJSON,el,fallback,urlPublica}from"./common.js";import{criarFiltros}from"./modules/filtros.js";import{criarCard}from"./modules/listagem.js";import{CATEGORIAS}from"./modules/filtros/definicoes.js";
const status=document.querySelector("#status"),categorias=document.querySelector("#categorias"),regioes=document.querySelector("#regioes");
for(const[value,label]of Object.entries(CATEGORIAS)){const a=el("a","atalho",label);a.href=`resultados.html?categoria=${encodeURIComponent(value)}`;categorias.append(a)}
for(const regiao of["Central","Norte","Sul","Leste","Oeste","Rural"]){const a=el("a","regiao",`Região ${regiao}`);a.href=`resultados.html?regiao=${encodeURIComponent(regiao)}`;regioes.append(a)}
try{const cards=await loadCards(),filtros=document.querySelector("#filtros");criarFiltros(filtros,cards,{modo:"full",aoAlterar:()=>{}});filtros.addEventListener("submit",()=>{location.href=`resultados.html${location.search}`});for(const[id,items]of[["destaques",cards.filter(x=>x.destaque).slice(0,6)],["recentes",cards.slice(0,6)]]){if(items.length){document.querySelector(`#${id}-secao`).hidden=false;items.forEach(item=>document.querySelector(`#${id}`).append(criarCard(item)))}}const clientes=await fetchJSON("dados/clientes/clientes.json");if(clientes.length){document.querySelector("#anunciantes-secao").hidden=false;for(const item of clientes.slice(0,3)){const article=el("article","card"),img=el("img");img.src=urlPublica(item.logo)||fallback;img.alt=item.logo?`Marca de ${item.nome}`:"Anunciante sem logotipo";const body=el("div","card-corpo"),h=el("h3"),a=el("a",null,item.nome);a.href=`cliente.html?id=${encodeURIComponent(item.id)}`;h.append(a);body.append(h,el("p",null,[item.tipo,item.cidade,item.uf].filter(Boolean).join(" · ")));article.append(img,body);document.querySelector("#anunciantes").append(article)}}status.textContent=cards.length?"":"Ainda não há imóveis publicados."}catch(error){status.textContent=error.message==="json"?"Os dados públicos estão em formato inválido.":"Não foi possível carregar o portal.";status.className="estado estado-erro"}
