import{money,fallback,el,precoDo,urlPublica}from"../common.js";import{favoritos}from"./favoritos.js";import{comparador}from"./comparador.js";
// A classe modo-lista ativa a apresentação horizontal em telas compatíveis.
const addMetric=(root,value,label)=>{if(Number(value)>0)root.append(el("span","metrica",`${value} ${label}`))};
function imagem(item){const img=el("img");img.loading="lazy";img.width=640;img.height=480;img.src=urlPublica(item.foto)||fallback;img.alt=item.foto&&item.titulo?`Foto de ${item.titulo}`:"Imóvel sem foto";img.addEventListener("error",()=>{img.src=fallback;img.alt="Imagem do imóvel indisponível"},{once:true});return img}
export function criarCard(item,{retorno=location.href,aoMudar=()=>{}}={}){const root=el("article",`card card-${item.tipo||"imovel"}`);root.append(imagem(item));const body=el("div","card-corpo"),top=el("small","card-tipo",`${item.finalidade||"Imóvel"} · ${item.subtipo||item.tipo||"Imóvel"}`),title=el("h3"),link=el("a",null,item.titulo||"Imóvel");link.href=`imovel.html?id=${encodeURIComponent(item.id)}&retorno=${encodeURIComponent(retorno)}`;title.append(link);body.append(top,title,el("p","localizacao",[item.bairro,item.location?.zone||item.regiao,item.cidade].filter(Boolean).join(" · ")),el("p","preco",money(precoDo(item))));const metrics=el("div","metricas");if(["casa","apartamento"].includes(item.tipo)){addMetric(metrics,item.quartos,"quartos");addMetric(metrics,item.suites,"suítes");addMetric(metrics,item.banheiros,"banheiros");addMetric(metrics,item.vagas,"vagas");addMetric(metrics,item.area_util||item.area,"m²")}else if(item.tipo==="terreno")addMetric(metrics,item.area_terreno||item.area,"m² de terreno");else if(item.tipo==="rural"&&item.area>0)addMetric(metrics,item.area>=10000?new Intl.NumberFormat("pt-BR",{maximumFractionDigits:2}).format(item.area/10000):item.area,item.area>=10000?"ha":"m²");else{addMetric(metrics,item.area_util||item.area,"m²");addMetric(metrics,item.banheiros,"banheiros");addMetric(metrics,item.vagas,"vagas")}body.append(metrics);
 const actions=el("div","acoes-card");[["favorito","Favoritar",favoritos],["comparar","Comparar",comparador]].forEach(([acao,label,store])=>{const b=el("button",`acao-${acao}`,label);b.type="button";b.setAttribute("aria-label",`${label} ${item.titulo||"imóvel"}`);b.setAttribute("aria-pressed",String(store.tem(item.id)));b.addEventListener("click",()=>{const ativo=store.alternar(item.id);b.setAttribute("aria-pressed",String(ativo));aoMudar(acao,item.id)});actions.append(b)});const share=el("button","acao-compartilhar","Compartilhar");share.type="button";share.setAttribute("aria-label",`Compartilhar ${item.titulo||"imóvel"}`);share.addEventListener("click",async()=>{if(navigator.share)await navigator.share({title:item.titulo,url:link.href});else{await navigator.clipboard?.writeText(link.href);share.textContent="Link copiado"}});actions.append(share);body.append(actions);root.append(body);return root}
export function criarListagem(root,{ordem="relevancia",aoMudar}={}){let modo=localStorage.getItem("imoveis:visualizacao")||"grade",itens=[];root.replaceChildren();const barra=el("div","barra-listagem"),count=el("strong","quantidade","0 imóveis"),label=el("label","sr-only","Ordenar imóveis"),sort=el("select","ordenacao");sort.id=`ordenacao-${Math.random().toString(36).slice(2)}`;label.htmlFor=sort.id;for(const[v,t]of[["relevancia","Relevância"],["menor-preco","Menor preço"],["maior-preco","Maior preço"],["maior-area","Maior área"]]){const option=el("option",null,t);option.value=v;sort.append(option)}sort.value=ordem;const toggle=el("div","alternador");toggle.setAttribute("role","group");toggle.setAttribute("aria-label","Visualização");for(const m of["grade","lista"]){const b=el("button",null,m==="grade"?"Grade":"Lista");b.type="button";b.dataset.modo=m;b.addEventListener("click",()=>{modo=m;localStorage.setItem("imoveis:visualizacao",m);render()});toggle.append(b)}const status=el("p","estado-listagem");status.setAttribute("role","status");status.setAttribute("aria-live","polite");const list=el("div",`cards modo-${modo}`);barra.append(count,label,sort,toggle);root.append(barra,status,list);
 function render(){sessionStorage.setItem("imoveis:resultado",JSON.stringify(itens.map(x=>x.id)));sessionStorage.setItem("imoveis:retorno",location.href);list.className=`cards modo-${modo}`;list.replaceChildren();toggle.querySelectorAll("button").forEach(b=>b.setAttribute("aria-pressed",String(b.dataset.modo===modo)));const data=[...itens];if(sort.value==="menor-preco")data.sort((a,b)=>precoDo(a)-precoDo(b));if(sort.value==="maior-preco")data.sort((a,b)=>precoDo(b)-precoDo(a));if(sort.value==="maior-area")data.sort((a,b)=>(b.area||0)-(a.area||0));count.textContent=`${data.length} ${data.length===1?"IMÓVEL":"IMÓVEIS"}`;status.textContent=data.length?"":"Nenhum imóvel corresponde à pesquisa.";for(const item of data)list.append(criarCard(item,{aoMudar}))}sort.addEventListener("change",render);return{mostrar(novos){itens=novos;render()},carregando(){status.textContent="Carregando imóveis…";list.replaceChildren(...[1,2,3].map(()=>el("div","skeleton")))},erro(mensagem="Não foi possível carregar os imóveis. Tente novamente."){status.textContent=mensagem;status.classList.add("estado","estado-erro");list.replaceChildren()}}}
