import{el,hide,mount,section}from"./util.js";export const normalizeAmenities=values=>[...new Set((values||[]).map(x=>String(x??"").replace(/\s+/g," ").trim().slice(0,120)).filter(Boolean))];export function initComodidades({imovel,root}){const values=normalizeAmenities(imovel?.amenities);if(!values.length)return hide(root);const node=section("Comodidades"),list=el("ul",{class:"comodidades"});for(const value of values)list.append(el("li",{},value));node.append(list);return mount(root,node)}
