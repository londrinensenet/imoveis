import{loadCards,el}from"./common.js";import{criarFiltros,ESTADO_INICIAL}from"./modules/filtros.js";import{criarListagem}from"./modules/listagem.js";
const listagem=criarListagem(document.querySelector("#listagem")),chips=document.querySelector("#chips"),titulo=document.querySelector("#titulo-busca"),resumo=document.querySelector("#resumo-busca");listagem.carregando();
const nomes={finalidade:"Finalidade",categoria:"Categoria",cidade:"Cidade",regiao:"Região",bairro:"Bairro",precoMin:"Preço mínimo",precoMax:"Preço máximo",quartos:"Quartos",vagas:"Vagas",features:"Comodidades"};
function atualizar(itens,estado,sugestoes){listagem.mostrar(itens);const finalidade=estado.finalidade==="comprar"?"para comprar":estado.finalidade==="alugar"?"para alugar":"",categoria=estado.categoria?`${estado.categoria[0].toUpperCase()+estado.categoria.slice(1)}s`:"Imóveis",local=estado.regiao?`na região ${estado.regiao} de ${estado.cidade||"Londrina"}`:`em ${estado.cidade||"Londrina"}`;titulo.textContent=[categoria,finalidade,local].filter(Boolean).join(" ");resumo.textContent=`${itens.length} ${itens.length===1?"imóvel encontrado":"imóveis encontrados"}`;chips.replaceChildren();for(const[key,value]of Object.entries(estado)){if(!value||value===ESTADO_INICIAL[key]||key==="ordem")continue;chips.append(el("span","chip",`${nomes[key]||key}: ${value}`))}const root=document.querySelector("#sugestoes");root.replaceChildren();for(const item of sugestoes)root.append(el("p",null,item.texto))}
try{const cards=await loadCards();criarFiltros(document.querySelector("#filtros"),cards,{modo:"compact",aoAlterar:atualizar})}catch(error){listagem.erro(error.message==="json"?"Os dados públicos estão em formato inválido.":undefined)}
