import{loadCards,el,fallback,money,precoDo,urlPublica}from"./common.js";import{comparador}from"./modules/comparador.js";
const root=document.querySelector("#comparacao"),limpar=document.querySelector("#limpar");let cards=[];const linhas=[["Finalidade",x=>x.finalidade],["Categoria",x=>x.subtipo||x.tipo],["Quartos",x=>x.quartos],["Banheiros",x=>x.banheiros],["Vagas",x=>x.vagas],["Área",x=>x.area_util||x.area],["Região",x=>x.location?.zone||x.regiao],["Bairro",x=>x.bairro]];
function render(){root.replaceChildren();const ids=comparador.listar(),itens=ids.map(id=>cards.find(x=>x.id===id)).filter(Boolean);limpar.hidden=!itens.length;if(!itens.length){const state=el("section","estado");state.append(el("h2",null,"Nenhum imóvel para comparar"),el("p",null,"Adicione imóveis pelos botões Comparar nos resultados."));root.append(state);return}const wrap=el("div","tabela-comparacao"),table=el("table"),caption=el("caption","sr-only","Comparação de imóveis"),thead=el("thead"),tr=el("tr");tr.append(el("th",null,"Campo"));for(const item of itens){const th=el("th"),img=el("img");img.src=urlPublica(item.foto)||fallback;img.alt=item.titulo?`Foto de ${item.titulo}`:"Imóvel sem foto";const title=el("a",null,item.titulo||"Imóvel");title.href=`imovel.html?id=${encodeURIComponent(item.id)}`;const price=el("p","preco",money(precoDo(item))),remove=el("button","botao botao-perigo","Remover");remove.type="button";remove.addEventListener("click",()=>{comparador.remover(item.id);render()});th.append(img,title,price,remove);tr.append(th)}thead.append(tr);const tbody=el("tbody");for(const[label,get]of linhas){const row=el("tr");row.append(el("th",null,label));for(const item of itens)row.append(el("td",null,get(item)||"—"));tbody.append(row)}table.append(caption,thead,tbody);wrap.append(table);root.append(wrap)}limpar.addEventListener("click",()=>{comparador.limpar();render()});try{cards=await loadCards();render()}catch{root.append(el("p","estado estado-erro","Não foi possível carregar os imóveis para comparação."))}
