import{escape,fallback}from"./common.js";const root=document.querySelector("#clientes"),status=document.querySelector("#status");try{const data=await fetch("dados/clientes/clientes.json").then(r=>{if(!r.ok)throw Error();return r.json()});root.innerHTML=data.map(x=>`<article class="card"><img src="${escape(x.logo||fallback)}" alt=""><div><h2><a href="cliente.html?id=${encodeURIComponent(x.id)}">${escape(x.nome)}</a></h2><p>${escape(x.tipo)} · CRECI ${escape(x.creci)}</p><p>${escape(x.cidade)} / ${escape(x.uf)}</p></div></article>`).join("");status.textContent=data.length?`${data.length} anunciantes`:"Nenhum anunciante disponível."}catch{status.textContent="Não foi possível carregar os anunciantes."}
